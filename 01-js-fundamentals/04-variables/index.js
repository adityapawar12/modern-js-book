let message;

message = 'Hello'; // store the string 'Hello' in the variable named message

let messageTwo = 'Hello!'; // define the variable and assign the value

// multiple variable declaration.
let user = 'John', age = 25, messageThree = 'Hello';

let userNew = 'John';
let ageNew = 25; 
let messageNew = 'Hello';

let hello = 'Hello world!';

let messageFour;

// copy 'Hello world' from hello into message
messageFour = hello;

let messageFive = "This";

// repeated 'let' leads to an error
// let messageFive = "That";

let $ = 1; // declared a variable with the name "$"
let _ = 2; // and now a variable with the name "_"

// cannot start with a digit
// let 1a; 

// hyphens '-' aren't allowed in the name
// let my-name; 

// note: no "use strict" in this example

num = 5; // the variable "num" is created if it didn't exist

// "use strict";

// num = 5; // error: num is not defined

// constants

const myBirthday = '14.04.2001';
// myBirthday = '01.01.2001'; // error, can't reassign the constant!

let admin, userName;

userName = "John";

admin = userName;

console.log('admin is >>> ', admin);
console.log('user name is >>> ', userName);

const ourPlanetName = "Earth";
const currentUserName = "Aditya Pawar";
